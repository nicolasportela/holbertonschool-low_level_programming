#include "holberton.h"

/**
 * is_prime_number - check the code for Holberton School students.
 * @n: variable name
 * Return: int.
 */

int prime



int is_prime_number(int n)
{
	if (n <= 1 || n % 2 == 0)
	{
		return (0);
	}
	else if (
	{
		return (1);
	}
}
