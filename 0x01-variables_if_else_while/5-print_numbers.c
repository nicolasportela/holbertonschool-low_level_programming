#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int n;
char a[10] = {"0123456789"};

for (n = 0; n < 10; n++)
{
putchar(a[n]);
}
{
putchar('\n');
}
return (0);
}
